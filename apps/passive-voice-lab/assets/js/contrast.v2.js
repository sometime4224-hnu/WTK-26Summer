export function contrastRatio(foreground, background) { const rgb=(hex)=>hex.replace("#","").match(/../g).map((part)=>parseInt(part,16)/255).map((value)=>value<=.04045?value/12.92:((value+.055)/1.055)**2.4);const luminance=(hex)=>{const [r,g,b]=rgb(hex);return .2126*r+.7152*g+.0722*b};const [a,b]=[luminance(foreground),luminance(background)].sort((x,y)=>y-x);return (a+.05)/(b+.05); }
